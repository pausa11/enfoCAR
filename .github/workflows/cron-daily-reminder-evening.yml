name: Daily Reminder - Evening

on:
  schedule:
    # Runs at 6:00 PM UTC-5 (23:00 UTC) every day
    - cron: '0 23 * * *'
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  send-reminder:
    runs-on: ubuntu-latest
    
    steps:
      - name: Send Evening Reminder
        run: |
          echo "💰 Sending evening reminder..."
          
          response=$(curl -s -w "\n%{http_code}" \
            -X GET \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/cron/daily-reminder")
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "Response: $body"
          
          if [ "$http_code" -eq 200 ]; then
            echo "✅ Successfully sent evening reminder"
          else
            echo "❌ Failed with status code: $http_code"
            exit 1
          fi
